import 'package:flutter/material.dart';

class CardUtile {
  IconData? icon;
  String? otherIcon;
  String name;
  int value;
  CardUtile(
      {this.icon, this.otherIcon, required this.name, required this.value});
}
